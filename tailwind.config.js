/** @type {import('tailwindcss').Config} */
export default {
  // In Tailwind CSS v4 the primary config is CSS-first via @theme in src/index.css.
  // This file is kept as a companion reference / for tools that still read tailwind.config.js.
  content: ['./index.html', './src/**/*.{js,jsx}'],
}
